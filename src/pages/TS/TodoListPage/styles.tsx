import { Box, styled } from '@mui/material';

export const StyledTaskColumnStyles = styled(Box)({
  margin: 8,
  display: 'flex',
  width: '100%',
  minHeight: '80vh',
});

export const StyledContainer = styled(Box)({
  display: 'flex',
  overflow: 'auto',
});
